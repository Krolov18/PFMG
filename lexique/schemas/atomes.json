{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Atomes",
  "description": "Description des attributs, catégories et valeurs d'une syntaxe",
  "$def": {
    "nonterminal": {
      "type": "string",
      "title": "Nonterminal",
      "description": "Symbole intermédiaire dans une règle syntaxique (un noeud, comme une variable s'il on veut)",
      "pattern": "^[A-Z]+$"
    },
    "terminal": {
      "type": "string",
      "title": "Terminal",
      "description": "mot final de la branche d'un arbre. (chaine de caractère terminale)",
      "pattern": "^'[a-z]+'$"
    },
    "attribut": {
      "type": "string",
      "title": "Attribut",
      "description": "Intitulé d'un ensemble de valeurs décrivant un critère descriptif",
      "pattern": "^[A-Z][A-Za-z]+$"
    },
    "catégorie": {
      "type": "string",
      "title": "Catégorie",
      "description": "Catégorie syntaxique",
      "pattern": "^[A-Z]+$"
    },
    "valeur": {
      "type": "string",
      "title": "Valeur",
      "description": "Critère descriptif faisant partie d'un attribut",
      "anyOf": [
        {
          "description": "Wildcard servant à dire que la valeur qui sera là devra être la même partout où '*' sera présent dans la production",
          "pattern": "^\\*$"
        },
        {
          "pattern": "^[a-z]+$"
        }
      ]
    },
    "noeud": {
      "type": "string",
      "title": "Noeud",
      "description": "Symbole nonterminal se trouvant à gauchedans une règle de production",
      "pattern": "^[A-Z]+P$"
    },
    "syntagmeOptionnel": {
      "type": "string",
      "title": "Syntagme optionnel",
      "description": "Syntagme qui peut ne pas apparaître",
      "anyOf": [
        {
          "$ref": "#/$def/terminal"
        },
        {
          "type": "string",
          "title": "Option",
          "description": "Syntaxe permettant de rendre un noeud syntaxique optionnel",
          "pattern": "^[A-Z]+/\\?$"
        }
      ]
    }
  }
}
